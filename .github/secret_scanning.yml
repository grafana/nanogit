paths-ignore:
  # Test-only signing fixtures (nanogit@grafana.test). Suppresses custom-pattern
  # alerts only; GitHub's default secret-scanning patterns will still fire and
  # need to be dismissed once as "used in tests".
  - "protocol/signing/testsigning/**"
